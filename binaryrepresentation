#include <stdio.h>

int main()
{
    int n,bit=31;
    scanf("%d",&n);
    for(bit=31;bit>=0;bit--)
    {
        if((1<<bit)&n)
        printf("1");
        else
        printf("0");
    }
    return 0;
}
